#include <stdio.h>

/**
 * main - what is my name?
 * @argc: total arguments
 * @argv: array whit arguments saved
 * Return: int
 */
int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);
	return (0);
}
