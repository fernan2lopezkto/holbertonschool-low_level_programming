#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/* here is my code */
	int x;

	for (x = 0; x < 10; x++)

	printf("%d", x);
	printf("\n");
	return (0);
}
