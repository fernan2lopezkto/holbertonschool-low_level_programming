#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)/*principal function*/
/*
 *imprimiremos en pantalla con puts / we will print to screen with puts
 */
{
	char TEXT[] = "\"Programming is like building a multilingual puzzle";

	puts(TEXT);
	return (0);
}
